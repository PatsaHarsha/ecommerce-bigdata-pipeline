# Dataset Directory

This folder is currently empty to comply with GitHub repository size limitations. 

To run the pipeline and notebooks, please copy your local `2019-Oct.csv` and `2019-Nov.csv` files into this directory. Once added, the Spark ingestion notebooks will automatically detect and process them using the configured relative pathing.
